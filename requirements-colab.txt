# Python deps for running SaShiMi+DiffWave on Colab (Pro, A100/L4).
# torch / torchaudio come preinstalled on Colab with a matching CUDA build —
# do NOT pip-install them here or you risk breaking the CUDA match.
#
#   pip install -r requirements-colab.txt
#
# pykeops gives the S4 layer a fast Cauchy kernel (10-50% faster training).
# Without it (and without the CUDA cauchy extension) S4 falls back to a slow path.
pykeops>=2.1
hydra-core>=1.2
omegaconf>=2.2
einops>=0.4
opt_einsum>=3.3
scipy>=1.7
tqdm>=4.62
# soundfile is a portable audio I/O fallback used by the music loader when
# torchaudio's backend is unavailable (e.g. torchaudio>=2.11 without torchcodec).
soundfile>=0.11
wandb>=0.12
# pytorch_lightning is imported by the vendored s4.py (only rank_zero_only is used).
pytorch_lightning>=1.6
